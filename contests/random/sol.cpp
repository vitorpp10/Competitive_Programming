#include <bits/stdc++.h>

#define ll long long
#define fastio \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;

bool func(ll x, ll i, ll limit) {
    
}

int main() {
    fastio;
    int tt;
    cin >> tt;
    while(tt--) {
        ll p,i,l;
        cin >> p >> i >> l;
        bool resp = func(p, i, l);
    }
    return 0;
}