#include <bits/stdc++.h>
#include <cstdio>

#define ll long long 
#define fastio \
    ios_base::sync_with_stdio(false); \
    cin.tie(NULL)

#define endl "\n"

using namespace std;

int main() {
    fastio;
    int x,y;
    while(cin >> x >> y) cout << (x * y) * 2 << endl;
    return 0;
}
